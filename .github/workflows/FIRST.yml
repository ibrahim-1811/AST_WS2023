name: FIRST

on:
  push:
  pull_request:

jobs:
  run-tests:
    strategy:
      fail-fast: false
    
    name: test
    runs-on: ubuntu-latest

    steps:
      - name: Check Code
        uses: actions/checkout@v2

      - name: install dep
        run: python -m pip install pytest

      - name: RUN TESTS
        run: pytest


 